{
  "name": "softsys-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "SoftSys Android wrapper (Capacitor)",
  "scripts": {
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:config": "powershell -ExecutionPolicy Bypass -File ./scripts/apply-android-config.ps1",
    "apk:debug": "npm run android:config && npm run android:sync && cd android && gradlew assembleLocalDebug",
    "apk:release": "npm run android:config && npm run android:sync && cd android && gradlew assembleLocalRelease",
    "apk:local:debug": "npm run android:config && npm run android:sync && cd android && gradlew assembleLocalDebug",
    "apk:local:release": "npm run android:config && npm run android:sync && cd android && gradlew assembleLocalRelease",
    "apk:casa:debug": "npm run android:config && npm run android:sync && cd android && gradlew assembleCasaDebug",
    "apk:casa:release": "npm run android:config && npm run android:sync && cd android && gradlew assembleCasaRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/core": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0",
    "typescript": "^5.8.0"
  }
}

